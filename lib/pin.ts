/**
 * Do dai ma PIN — dung o ca client (ban phim so) lan server (kiem tra).
 *
 * Tach khoi lib/auth.ts vi auth.ts doc cookie va DB nen chi chay duoc o server;
 * import no vao component client se keo ca DB sang phia trinh duyet.
 */
export const PIN_LEN = 4;
